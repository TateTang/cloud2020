server:
  port: 9005
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置地址
management:
  endpoints:
    web:
      exposure:
        include: "*"
